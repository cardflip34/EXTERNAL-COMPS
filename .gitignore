# NEVER publish comp-store dumps, evidence, cookies, tokens, env, logs, buyer/bidder data
*.json
!external_engine/**/*.schema.json
!reports/**/*.json
*.jsonl
*.db
*.duckdb
*.parquet
*.log
*.out
*.bak*
.env*
*cookie*
*token*
*secret*
browser_state/
__pycache__/
*.pyc
.DS_Store
.venv*/

# credentials that live next to synced ops scripts — never let one slip in
.jina_key
*jina_key*
*.key
*.pem
*_token*
